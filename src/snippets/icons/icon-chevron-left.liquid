{%-liquid
  assign width = width | default: 6
  assign height = height | default: 10
  assign fill = fill | default: "#323332"
-%}

<svg width="{{ width }}" height="{{ height }}" viewBox="0 0 6 10" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path d="M5 1L1 5L5 9" stroke="{{ fill }}" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
